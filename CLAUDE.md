# CLAUDE.md

## トークン消費のバランス(全プロジェクト共通)

出典: [taiyo5424-star/unyou](https://github.com/taiyo5424-star/unyou) の `docs/operations/quota-management.md`(詳細・最新はそちら)。

- **クラウドセッションで実際に効く節約策**: 最上位モデル(Fable等)のセッションでは、Fableは計画・判断・監査・検収に徹し、実行作業(コード生成・長文執筆・定型処理)は安いモデルのサブエージェントに委譲する(Opus=作る / Sonnet=回す / Haiku=さばく)。サブエージェントの消費はそのモデルの枠に付き、Fable枠には乗らないため、委譲するほどFable枠が残る。
- 定型作業は低エフォートで行う。高エフォートは監査・設計など深く考えるほど効くタスクに限定する。
- ユーザーから利用残量を共有されたら、運転モードを宣言してから作業する: 残量50%以上=通常運用 / 25〜50%=節約運用(委譲徹底・応答簡潔) / 25%未満=温存運用(Fableは短い判断のみ、実行は安いモデルかリセット後に回す)。
- **Codex等の外部エージェントへの委譲はユーザーのローカル環境専用**。クラウドセッションからは呼び出せない(別ベンダー認証がないため)。ローカルで使う場合は同じフォルダを開いて計画書だけを渡す。
- Fable 5のサブスク内提供は2026-07-20以降Max・Team Premiumのみ(週次50%上限)。ユーザーはMaxのため継続利用可。
- 完了報告は実際の実行結果を根拠に行う。推測の報告はしない。
