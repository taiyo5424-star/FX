# 08. ユーザー運用ランブック(2026-07-18版)

あなた(ユーザー)が行うこと/自動で行われること/待っているだけでよいことの整理。

## 現在の状態を一言で

**APPROVED戦略0件 → ライブ発注は全面NO_TRADE。**
検証を通らない戦略で取引しないことが、現時点で最も収益に貢献する行動です
(9候補すべてが厳格検証で不合格=市場にただ乗りできる単純エッジは現存しない)。

## あなたにお願いしたいこと(優先順)

### 1. 実測スプレッド収集(最重要・所要1回2分)
- 松井証券FXの取引画面を開き、`スプレッド記録.bat` を実行(手順書_スプレッド収集.md)
- 特に価値が高い時間帯: 東京9:00-10:00 / ロンドン16:00-17:00 / NY21:00-23:00 /
  **早朝5:00-8:00(「原則固定」の例外時間帯。Dukascopy実測では最大10倍拡大)**
- 週3回×2週間で最低限の分布が取れます。これが将来のペーパートレード判定の前提です。

### 2. config/risk_limits.json の初期値承認
- 内容を確認し、問題なければ `approved_by_user: true` に変更してコミット
  (またはセッションで「承認する」と伝える)。

### 3. 月次レビューの確認(毎月1日に自動生成)
- 自動レポートに目を通し、方針変更の要否だけ判断。

## 自動で行われること(Routine)

| Routine | 頻度 | 内容 |
|---|---|---|
| fx-monthly-review | 毎月1日 9:00 JST | 市場環境更新・C017解禁判定・レビュー生成・push |
| fx-mof-intervention-update | 2026-08-08 9:00 JST(1回) | 財務省の介入日次内訳を反映 |
| fx-weekly-forward-test | 毎週土曜 9:00 JST | C009/C009v2の前進検証を新データで更新 |

## 便利ツール

```bash
# ポジションサイズ計算(発注前に必ず)
python3 scripts/ops/position_size.py --sl 25

# 介入/急変チェック(30分150pips超で24時間NO_TRADE判定)
python3 scripts/ops/intervention_detector.py

# 前進検証の手動更新
python3 scripts/ops/forward_test.py
```

## 今後の道筋(収益化までの正直な地図)

1. **〜2026-08**: スプレッド実測蓄積(あなた)+前進検証の自動蓄積(Routine)
2. **2026-08-08**: 介入日次内訳の反映 → 介入検知器の閾値再検証
3. **2026-Q4〜2027-01-05**: C017(金利残差モメンタム)を新データで検証解禁
4. **合格候補が出たら**: PAPER_TRADE(20シグナル)→ あなたの承認 → MICRO_LIVE
   (リスク1,250円/取引)→ 実測が想定内なら通常運用へ段階昇格
5. **どの段階でも**: 発注はあなたの操作(松井証券の規程によりbot発注は不可)

## してはいけないこと(再確認)

- 検証不合格・未検証の手法での「試し玉」(検証プロセスの意味が消えます)
- ナンピン・マーチンゲール・損切り先延ばし(config/risk_limits.json prohibited)
- 日次損失20,000円(警戒16,000円)・1日3取引・4連敗停止の無視
